[Unit]
Description=Redeploy Docker stack on homelab after git sync
After=git-sync.service
Requires=git-sync.service

[Service]
Type=oneshot
WorkingDirectory=/path/to/home-lab/homelab
ExecStart=/bin/bash -c 'find . -name "docker-compose.yml" -execdir docker compose up -d \;'
User=your-user
Group=docker

[Unit]
Description=Redeploy Docker stack on edge after git sync
After=git-sync.service
Requires=git-sync.service

[Service]
Type=oneshot
WorkingDirectory=/path/to/home-lab/edge
ExecStart=/usr/bin/docker compose up -d
